// main.js - 可添加通用的前端逻辑或交互
console.log("main.js loaded.");

// 你也可以在此写 Ajax 请求，比如轮询后端新的数据等
// Example:
// setInterval(() => {
//   fetch('/QueryData?mode=latest&deviceId=raspi001')
//     .then(res => res.text())
//     .then(html => {
//       // 解析或更新 DOM
//     });
// }, 60000);
